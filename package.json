{
  "name": "email-adapter",
  "version": "1.0.0",
  "scripts": {
    "start": "python app.py",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "14.x"
  }
}
